apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: session-db-pvc
  namespace: sock-shop
spec:
  accessModes:
    - ReadWriteOnce          # 单节点读写
  resources:
    requests:
      storage: 5Gi           # 5G 足够测试用
  storageClassName: local-path
